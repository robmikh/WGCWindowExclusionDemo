name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

env:
  PACKAGES_CONFIG_PATH: WGCWindowExclusionDemo\packages.config
  SOLUTION_FILE_PATH: WGCWindowExclusionDemo.slnx

jobs:
  build:
    name: Build ${{ matrix.architecture }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: amd64
            msbuild_architecture: x64
            platform: x64
            runner: windows-2025
          - architecture: arm64
            msbuild_architecture: x64
            platform: ARM64
            runner: windows-2025

    steps:
      - name: Check out release tag
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.ref }}

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@30375c66a4eea26614e0d39710365f22f8b0af57 # v3
        with:
          msbuild-architecture: ${{ matrix.msbuild_architecture }}

      - name: Restore NuGet packages
        run: >
          nuget restore $env:PACKAGES_CONFIG_PATH
          -PackagesDirectory .\packages
          -NonInteractive

      - name: Build release binary
        run: >
          msbuild $env:SOLUTION_FILE_PATH
          /m
          /p:Configuration=Release
          /p:Platform=${{ matrix.platform }}
          /p:OutDir="$env:GITHUB_WORKSPACE\out\${{ matrix.architecture }}\"

      - name: Package release binary
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
        run: |
          $binary = "out\${{ matrix.architecture }}\WGCWindowExclusionDemo.exe"
          if (-not (Test-Path -LiteralPath $binary -PathType Leaf)) {
            throw "Expected release binary was not produced: $binary"
          }

          $packageName = "WGCWindowExclusionDemo-$env:RELEASE_VERSION-${{ matrix.architecture }}"
          $packageDirectory = Join-Path $env:RUNNER_TEMP $packageName
          New-Item -ItemType Directory -Path $packageDirectory | Out-Null
          Copy-Item -LiteralPath $binary, "README.md", "LICENSE.txt" -Destination $packageDirectory

          New-Item -ItemType Directory -Path "dist" | Out-Null
          Compress-Archive `
            -Path "$packageDirectory\*" `
            -DestinationPath "dist\$packageName.zip"

      - name: Upload packaged binary
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: WGCWindowExclusionDemo-release-${{ matrix.architecture }}
          path: dist\WGCWindowExclusionDemo-*.zip
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  publish:
    name: Publish GitHub release
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
      id-token: write
      attestations: write
      artifact-metadata: write

    steps:
      - name: Download packaged binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: WGCWindowExclusionDemo-release-*
          path: dist
          merge-multiple: true

      - name: Generate checksums
        shell: bash
        run: |
          cd dist
          sha256sum -- *.zip > SHA256SUMS.txt

      - name: Attest release assets
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: |
            dist/*.zip
            dist/SHA256SUMS.txt

      - name: Create or update GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          mapfile -t assets < <(
            find dist -maxdepth 1 -type f \
              \( -name '*.zip' -o -name 'SHA256SUMS.txt' \) \
              -print | sort
          )
          if (( ${#assets[@]} < 3 )); then
            echo "Expected two architecture archives and a checksum file." >&2
            exit 1
          fi

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "${assets[@]}" --clobber
          else
            prerelease=()
            if [[ "$TAG" == *-* ]]; then
              prerelease+=(--prerelease)
            fi
            gh release create "$TAG" "${assets[@]}" \
              --verify-tag \
              --generate-notes \
              --title "$TAG" \
              "${prerelease[@]}"
          fi
